services:
  letscode-postgres:
    image: postgres:17
    container_name: letscode_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres123
      POSTGRES_DB: letscode
    ports:
      - "5432:5432"